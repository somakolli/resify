name: resify

services:
- name: customer-ui
  gitlab: 
    repo: freshspark/resify
    branch: master
    deploy_on_push: true
  source_dir: frontend
  routes:
  - path: /
  dockerfile_path: Dockerfile_customer-ui

